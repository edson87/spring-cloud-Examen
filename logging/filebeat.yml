filebeat.inputs:
  - type: log
    enabled: true
    paths:
     - /host-logs/*.log
    fields:
      service: producto-service
  - type: log
    enabled: true
    paths:
     - /host-logs2/*.log
    fields:
      service: orden-service
  - type: log
    enabled: true
    paths:
      - /host-logs3/*.log
    fields:
      service: inventario-service
output.logstash:
  hosts: ["logstash:5044"]
